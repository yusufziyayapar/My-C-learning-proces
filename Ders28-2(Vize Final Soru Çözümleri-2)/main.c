#include <stdio.h>

int main() {
/*Klavyeden otoparkta kalınan saati girdiğinde tutarı gösteren programı
 * kodlayınız.
 * 0-4 saat:10 TL 5-8 saat:12 Tl 9-12 saat: 15 TL 13-... saat: 20 TL
  int saat;

    printf("Kaldiginiz saat: ");
    scanf("%d",&saat);

        if(saat>0 && saat<=4){
            printf("odemeniz gereken tutar: 10 TL");
        }
        else if(saat>=5 && saat<=8)
        {
            printf("odemeniz gereken tutar: 12 TL");
        }
        else if(saat>=9 && saat<=12)
        {
            printf("odemeniz gereken tutar: 15 TL");
        }
        else if(saat>=13)
        {
            printf("odemeniz gereken tutar: 20 TL");
        }
// başka bir çözüm yolu
    int saat;
    printf("Kac saat kaldiniz: ");
    scanf("%d",&saat);
    switch(saat)
    {
        case 0 ... 4:
            printf("10 tl odeyin");break;
        case 5 ... 8:
            printf("12tl odeyin");break;
        case 9 ... 12:
            printf("15 tl odeyin");break;
        default :
            printf("20 tl odeyin");break;
    } */
//----------------------------------------------------------------------



    return 0;
}
