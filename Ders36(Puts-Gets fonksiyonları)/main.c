#include <stdio.h>

int main() {
// puts ve gets fonksiyonları

char bilgi[40];

    printf("Bilgiyi girin: ");
    gets(bilgi); // scan yerine gets
    printf("\n\n");
    puts(bilgi); // printf ile yazdırmak yerine puts ile yazdırıyoruz






    return 0;
}
